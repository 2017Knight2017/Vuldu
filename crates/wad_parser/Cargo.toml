[package]
name = "wad_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "1.25.0", features = ["derive"] }
image = "0.25.10"
rayon = "1.12.0"
renderer = { path = "../renderer" }
